dist/
node_modules/
.astro/

# Generated by scripts/prepare-assets.mjs before every build: the image assets
# an enumerated SKILL.md embeds, copied out of the plugin trees so an <img> has
# something to fetch. Generated, therefore not committed — the sources live in
# plugins/<p>/skills/<s>/assets/ and are the only copy anything depends on.
public/skill-assets/
